{
  "name": "oahl",
  "version": "0.1.1",
  "private": false,
  "description": "Open Agent Hardware Layer (OAHL) monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "start": "npm run start --workspace=@oahl/server"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@oahl/adapter-android": "^0.1.1"
  }
}
